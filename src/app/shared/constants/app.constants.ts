/** Maximum number of tabs that can be saved in a single session. */
export const MAX_TABS_PER_SESSION = 500;

/** Maximum character length for a session name. */
export const MAX_SESSION_NAME_LENGTH = 100;

/** Default number of sessions shown in the recent-sessions list. */
export const DEFAULT_RECENT_SESSIONS_LIMIT = 10;

/** Semantic version of this extension build. */
export const EXTENSION_VERSION = '1.0.0';
